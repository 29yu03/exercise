<div class="container">
  <div class="row">
    <div class="col">
      <h3 class="text-center pt-5 pb-3"><%= @user.full_name %></h3>
      <% provide(:title, @title) %>
      <h5 class="text-center pb-5"><%= @title %></h5>
      <div class="col pl-5">
      <%= render @users %>
      </div>
      <%= link_to '戻る', user_path(@user) %>
    </div>
  </div>
</div>