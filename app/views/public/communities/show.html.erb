<div class='container'>
<h2>Group Detail</h2>
<table class="table table-hover table-inverse">
  <thead>
    <tr>
      <th></th>
      <th>グループ名</th>
      <th>紹介文</th>
      <th>メンバー</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
      <tr>
        <td><%= @community.name %></td>
        <td><%= @community.introduction %></td>

        <td>
            <!-- @groupは中間テーブルを挟んでusersテーブルを見ている。-->
            <% @community.users.each do |member| %>
              <li style="list-style: none;"><%= member.last_name %><%= member.first_name %></li>
            <% end %>
          </td>
          <td>
            <!-- @groupに紐づいたgroup_user（user_idカラムに自分のIDを持っている）があるかどうかを確かめる。-->
            <% if @community.group_users.exists?(user_id: current_user.id) %>
              <!-- group_usersコントローラーの#destroyアクションを発動　@groupからgroup_user(自分)を消す -->
              <%= link_to 'グループから退会する', community_group_users_path(@community), method: :delete, class: "btn btn-sm btn-danger" %>
            <% else %>
            <!-- group_usersコントローラーの#createアクションを発動　@groupにgroup_user(自分)を追加する -->
              <%= link_to 'グループに参加する', community_group_users_path(@community), method: :post, class: "btn btn-sm btn-success" %>
            <% end %>
          </td>

        <% if @community.owner_id == current_user.id %>
          <td>
            <%= link_to 'Edit', edit_community_path(@community), class: "btn btn-sm btn-success" %>
          </td>
        <% end %>
      </tr>
  </tbody>
</table>
</div>
