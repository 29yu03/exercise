<%= form_with(model: [@topic, @comment], url: community_topic_comments_path(@community, @topic), local: false ) do |f| %>
    <%= f.text_area :body, rows: '5', placeholder: "コメントをここに", id: "comment_textarea" %>
    <%= f.submit "送信する" %>
<% end %>